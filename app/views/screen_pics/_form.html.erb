<%= form_for @screen_pic, :html => { :multipart => true } do |f| %>
  <% if @screen_pic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@screen_pic.errors.count, "error") %> prohibited this screen_pic from being saved:</h2>

      <ul>
      <% @screen_pic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
